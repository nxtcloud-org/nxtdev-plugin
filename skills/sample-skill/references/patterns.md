# Reference Patterns

This file is loaded on-demand when Claude needs detailed reference material.

## Search Patterns

- File search: Use `Glob` with patterns like `**/*.ts`
- Content search: Use `Grep` with regex patterns
- Read files: Use `Read` for known paths
